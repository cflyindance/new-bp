# release/5.11.82.2 增量核查

## 来源与范围

- 分支：`release/5.11.82.2`
- 上次处理 SHA：`befc9f78ed00265789dd948b9005886d821ac647`
- 当前 release SHA：`9c6fd1e1938f691bdaa5f9f3059959fcbd5efe3f`
- Git 祖先检查确认上次处理 SHA 是当前 SHA 的祖先。
- 增量共 6 个提交，代码差异为 5 个文件、增加 50 行、删除 4 行。

## 已验证变化

### 认证信息写入会通知 useWS

- `src/utils/storage.js` 在写入 `emenu_auth` 后派发 `emenu_auth_changed` 自定义事件，事件 `detail` 为写入值。
- `src/hooks/useWS.js` 监听该事件并调用 `setAuthInfo(event.detail)`，卸载时移除监听。

### SocketCenter 关闭时释放客户端引用

- `src/hooks/useSocketCenter.js` 包装新建客户端的 `close` 方法。
- 关闭当前客户端时，代码会清空指向该客户端的 `window.globalWs` 与 `scRef.current`，再调用原始关闭方法。

### CDS 呼叫服务消息增加来源

- `src/services/system.js` 的 `sendCdsMsg` 请求体新增 `requestSource: 'EMENU_CALL_SERVER'`。

### License 占用提示补充设备标识

- `src/hooks/useClientLogin.js` 重命名为 `src/hooks/useClientLogin.jsx`，以承载提示中的 JSX。
- 发生 license 已绑定校验失败时，提示展示绑定设备名称、绑定设备 ID 和当前设备 ID。
- 两个设备 ID 有值时只展示前 4 位和后 4 位，中间替换为 `****`。

## 稳定文档更新

- 新增 [`../flows/2026-07-13-认证状态与消息连接流程.md`](../flows/2026-07-13-认证状态与消息连接流程.md)，记录可从本次代码差异确认的认证状态、连接引用和 CDS 消息流程。

## 验证与限制

- 事实来源为上述 SHA 区间的 Git 提交、文件列表和代码差异。
- 本次没有运行浏览器或后端联调，因此未把运行时连接恢复结果、事件跨窗口行为或后端对 `requestSource` 的处理方式写成已验证事实。
