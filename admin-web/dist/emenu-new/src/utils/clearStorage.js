export const clearAllStorage = () => {
  // for (let i = 0; i < localStorage.length; i++) {
  //   const key = localStorage.key(i)
  //   if (key.startsWith('emenu_')) {
  //     localStorage.removeItem(key)
  //   }
  // }
  localStorage.clear()
}
