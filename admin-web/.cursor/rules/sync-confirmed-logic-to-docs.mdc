---
description: 逻辑方案确认并实现后同步更新对应设计文档
alwaysApply: true
---

# 已确认逻辑同步文档

当用户确认业务逻辑、交互流程、字段语义或数据规则的调整后：

1. 实现变更时同步更新对应的 `docs/` 设计、规格或说明文档，不等待用户再次提醒。
2. 文档须直接更新对应功能域唯一权威文件 `docs/superpowers/specs/<feature-slug>/README.md`（见 `feature-design-docs-organization`），不得为同一功能新建日期型 specs 设计稿。
3. README 正文只保留当前有效方案；旧文档与新方案冲突时，将必要历史压缩到“变更记录”，并让旧文件指向 README。
4. 文档必须记录用户确认后的最终口径，包括展示条件、字段语义、异常处理和验收标准。
5. 仅同步与本次逻辑变更直接相关的文档，不进行无关文档重写。
6. `docs/superpowers/plans/` 实施计划继续单独保存，并与功能 README 建立相对链接，不合并进 specs 正文。
7. 完成回复中说明已更新的权威 README；若仓库中没有对应文档，应按项目约定创建功能域目录及唯一 README。
