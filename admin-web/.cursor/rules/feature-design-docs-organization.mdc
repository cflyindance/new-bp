---
description: 同一功能域的 specs 设计只维护一份 README 权威文档
alwaysApply: true
---

# 功能设计文档单一权威文件

`docs/superpowers/specs/` 中，同一功能域只能维护一份权威设计文档。后续新增需求、交互调整、字段变更和规则修订必须直接更新该文件，禁止为同一功能重复创建日期型设计稿。

## 唯一目录与文件

```text
docs/superpowers/
├── specs/
│   └── <feature-slug>/
│       ├── README.md          # 该功能唯一 specs 权威文档（SSOT）
│       └── assets/            # 可选：图片、样例等非设计 Markdown 附件
└── plans/
    └── YYYY-MM-DD-<topic>-implementation.md
```

- 功能目录使用 kebab-case，例如 `menu-order-limit/`、`duration-billing/`。
- `README.md` 是该功能全部 specs 设计内容的唯一事实来源。
- `plans/` 中的实施计划不合并进 README，可按任务独立创建，并链接到对应功能 README。
- PRD/SPEC 导出文件继续存放于 `docs/产品PRD/exports/`，在 README 中以相对链接引用，不复制正文。

## “同一功能”的判断边界

满足以下多数条件时视为同一功能，应合并到同一 README：

- 服务同一个用户目标或业务对象；
- 使用同一套核心数据模型或规则实体；
- 位于同一主入口、页面流程或配置入口；
- 后续需求是在扩展、修正或替代现有功能，而非建立独立能力；
- 验收必须与现有功能一起判断才完整。

仅属于同一个大业务域、但入口、数据模型、生命周期和验收均可独立的能力，应建立不同功能 README。跨功能共享模型或约定建立独立共享能力 README，并由各功能引用。无法可靠判断是否属于同一功能时，必须先询问用户，不得为了减少文件而强行合并。

## README 必备结构

每个功能域的 `README.md` 至少包含：

1. 功能范围与目标
2. 当前有效口径
3. 页面与交互设计
4. 字段、数据模型与存储
5. 业务规则与异常处理
6. 关联模块及上下游影响
7. 验收标准
8. 关联 PRD、SPEC 与实施计划
9. 变更记录

章节可按功能复杂度调整，但不得另建第二份设计 Markdown 分担权威内容。

## 新增和更新规则

1. **新功能**：创建 `specs/<feature-slug>/README.md`，将完整设计写入该文件。
2. **已有功能**：定位现有功能域 README，直接修改对应章节并更新“当前有效口径”和“变更记录”。
3. **同功能追加需求**：不得新建 `YYYY-MM-DD-<topic>-design.md`；必须合并到同一 README。
4. **实施计划**：继续写入 `docs/superpowers/plans/`，计划开头链接权威 README；README 的“关联实施计划”同步登记链接。
5. **历史变化**：正文只保留当前有效方案；旧口径在“变更记录”中简要记录日期、变化与替代原因，避免复制整份历史正文。
6. **跨功能需求**：分别更新各功能域 README；真正共享的能力建立独立功能域，不把多个无关功能塞入同一文件。

## 旧文档合并

发现同一功能已有多份 specs 设计稿时：

1. 按以下证据优先级识别当前有效口径：用户已确认的最新口径 > 已实现且有测试覆盖的行为 > 最新权威文档 > 较早设计稿。
2. 将有效内容合并到该功能域 `README.md`，消除相互冲突和重复描述。
3. 将必要的历史决策压缩进 README 的“变更记录”。
4. 若实现、测试和文档互相冲突且无法从用户确认记录判定，停止合并并询问用户，不得猜测或丢弃任一口径。
5. 更新代码注释、plans 和其他维护中文档中的旧 specs 引用，使其指向 README。对已导出的 PRD/SPEC，仅在其中本来就存在旧 specs 链接且用户要求维护导出物时修复链接；不得为建立反向链接而普遍改写导出产物。
6. 日常文档同步不自动授权删除旧文件。没有用户明确删除授权时，只在旧文件顶部标记“已废弃”并链接 README，不得继续维护。
7. 获得明确删除授权后，删除前仍须检查 Git 状态、确认目标文件无未合并的用户改动，并用全仓引用搜索验证所有旧路径均已更新；任一条件无法确认时不删除，只保留废弃标记。

## 禁止

- ❌ 在 `docs/superpowers/specs/` 根目录新建同功能日期型设计稿
- ❌ 在功能目录内同时维护 README 与多个 `*-design.md`
- ❌ 为一次小改动复制整份设计文档
- ❌ 让 README 只做索引、把真实设计分散到其他 specs Markdown
- ❌ 将实施步骤、任务拆分等 plans 内容强制并入 README
- ❌ 删除旧文档前不更新引用或不确认有效内容已合并
- ❌ 将 alwaysApply 规则视为删除历史文件的用户授权

## 执行检查

创建或修改 specs 前必须确认：

- 是否已存在对应功能域 README；
- 本次内容是否应直接更新现有章节；
- 是否意外产生第二份 specs Markdown；
- 当前有效口径、验收标准和变更记录是否同步；
- 新增或变更的实施计划是否已与 README 互相链接。
