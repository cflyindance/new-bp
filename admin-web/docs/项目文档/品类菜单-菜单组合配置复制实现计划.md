# 品类菜单 · 菜单组合配置复制 · 实现计划

> **依据**：`docs/项目文档/品类菜单-菜单组合配置复制设计方案.md`  
> **日期**：2026-07-24  
> **状态**：已实现

## 任务拆分

### 1. 结构并集工具

`brand-menu-structure-picker-ui.ts`：

- `cloneBrandMenuStructureByLine`
- `mergeBrandMenuStructureByLine`（按产线 keys 并集去重）

### 2. 复制纯函数

`foh-category-settings-ui.ts`：

- `applyMenuComboCopy(source, target, targetKey, mode, validKeys)`
- 覆盖：可下单/不可下单字段深拷贝；`viewOnlyCategoryKeys` 过滤自身
- 合并：可下单并集；不可下单以源模式为准再合并对应字段

### 3. 表格操作列

- 表头增加「操作」
- 行增加「复制」按钮 `data-foh-menu-combo-copy`

### 4. 复制弹窗

- `renderMenuCopyDialogShell` + body（目标多选、合并/覆盖）
- 未选目标时「确认复制」禁用
- 无其他组合时空态文案

### 5. 事件

- 打开 / 取消 / 确认 / Esc
- 确认后 `writeFohCategorySettingsState` → remount

## 冒烟验收

对照设计方案 §六。
