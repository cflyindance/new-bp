# 薪资修改合入 main

## 范围

- 仅提取薪资周期、演示场景、考勤视角、设置页和滚动修复，以及相关文档、测试和薪资 mock API 修订控制。
- 不包含其他模块、构建产物、全局预览配置；不推送远端。
- 基于 main `78cd22ed`，提取来源 `7ad753fc`，保留 main 的原生打印、详细/精简明细与跨年期数实现。

## 冲突处理

- 实际旧双周期数据继续使用 main 的跨年迁移；带 frequency/scheduleRuleId 的新周期保留日期、身份及选择，不进入旧迁移。
- 示例年份取自示例数据；实际年份保留 main 的计算。
- 员工导出保留 main 的打卡字段，并支持动态周数。
- 恢复 main 最近模板更新中遗漏的已有批量导出模板/样式和 ADP 确认弹框层级。

## 验证

- TypeScript 类型检查通过。
- 默认场景、弹框居中、筛选滚动、周期保留、Regular/OT、revision、全屏设置、设置隔离、工作周测试通过。
- main 的双明细、跨年周期、ADP 层级、批量导出 UI 和薪资页面结构回归通过。
- 场景生成、周期引擎、历史生命周期、锁定保护、批量导出领域测试通过。
- 未重新进行合并版本的完整浏览器验收或生产构建；不将上述静态/领域检查视为端到端验收。
- 正式周期规则发布仍未接入；本次不扩展其业务边界。
