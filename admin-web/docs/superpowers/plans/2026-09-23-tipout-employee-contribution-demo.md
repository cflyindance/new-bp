# 员工贡献入池演示补充

用户确认：服务员贡献、调酒师贡献并接收、小费不足封顶三个场景。仅影响新生成的演示事实和新保存的快照，历史快照不迁移。

实现：B01 中 Server 销售额1000、Bartender销售额600，按8天周期交替提供小费60或1，正常打卡8小时。保留 B01–B08 覆盖与订单金额勾稽；测试场景清单说明标注案例。

新快照增加 employeeContributions，复用 runDeductPipeline 按规则顺序计算，跨规则不得超过剩余小费。员工汇总优先读取该字段，不再用员工固定比例代替；旧快照保持旧读取路径，不写回。

- [x] 验证两类角色均有正常贡献与封顶事实。
- [x] 验证多个规则累计贡献封顶及非贡献角色为零。
- [x] 类型检查通过。
- [x] 浏览器验证新字段保存及原有分配、编辑回归。

演示使用方式：先进入规则列表确保演示规则已补齐，再对未保存快照的日期分配。已有日期仅在用户显式更新/重新确认分配后产生新结果；已发放不可修改。
