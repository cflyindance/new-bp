# “开单前，换桌”按产线三态合并实施计划

> 设计依据：`docs/superpowers/specs/2026-08-11-pre-order-table-change-by-line-merge-design.md`

## 目标

以 seq 643 为唯一可见设置，将 seq 643/644 合并为 POS、POS GO、PayPad、eMenu、SDI 五条产线的“不启用 / 可选换桌 / 必须换桌”三态矩阵，并兼容旧数组和旧总开关。

## 实施步骤

1. 新增聚焦验证脚本，先覆盖目录合并、五产线范围、三态迁移、镜像派生、全部关闭及布尔写入拦截，保存 RED 结果。
2. 重构 `module-settings-pre-order-table-change-ui.ts`：实现三态主对象、无副作用旧数据解码、迁移、镜像同步、专用按产线读写接口和矩阵渲染。
3. 调整 `main.ts`：seq 643 使用无总开关的专用三态行，移除 seq 644 可见分派与旧面板显隐副作用。
4. 调整通用开关、按产线和 AI 改配边界：seq 643/644 不再走布尔写入；seq 643 的布尔读取仅作为兼容投影。
5. 更新目录生成来源、排除项、分组顺序、标题说明和产线 scope；保留 seq 644 兼容 storage registry。
6. 运行生成脚本并审查差异，只接受 seq 643/644 及必要基线范围的变更。
7. 运行聚焦验证、前厅 scope/storage/22 组回归、TypeScript 检查和生产构建。
8. 在浏览器验证场景视图、代表性按产线视图、三态保存/刷新、全部关闭和控制台。

## 变更隔离

- 只补丁修改目标代码段，不覆盖当前工作区其他未提交变更。
- 生成前后比较目标文件；出现无关漂移立即停止并人工核对。
- 不提交、不推送，除非用户另行要求。
