# 自助餐规则全屏流程实施计划

1. 增加静态验证，要求自助餐 editor/publish 页面集合、列表退出、共享控制器与主应用绑定存在。
2. 抽取 `rule-iframe-fullscreen.ts`，统一管理唯一 owner、iframe/文档样式快照、滚动位置、fail-safe 页面识别和幂等 teardown。
3. 菜单下单限制改用共享控制器，保持现有步骤页全屏和列表退出行为。
4. 自助餐规则 iframe 增加专用选择器并接入相同控制器；主应用挂载时绑定，重挂载和离开模块时释放。
5. 运行静态回归、TypeScript/生产构建和浏览器新增→全屏→返回列表验证。
