# 发薪周期设置全屏实施计划

**Goal:** 按用户确认，将周期设置改为全屏页面，返回保留薪资上下文。

**Architecture:** 使用原生 dialog 顶层全屏容器隔离背景交互，不销毁薪资页面；新增、编辑、确认保持居中弹框。

**Tech Stack:** TypeScript、Shadow DOM、CSS。

**Spec:** 本任务用户已确认的五项交互范围。

## Global Constraints

- 不改变规则保存权限，不写入薪资数据。
- 返回保留门店、年度、期数及员工。

## 实施与验证

- [ ] 为设置容器的独立全屏类、返回文案及 CSS 写源码契约测试，先运行失败。
- [ ] 设置 panel 类增加 payroll-schedule-page；页头返回按钮沿用 data-close；独立 CSS 覆盖宽高、边距、圆角并限制内容宽度。
- [ ] 执行测试与 tsc；浏览器核对全屏覆盖、滚动、新增弹框与返回状态。
- [ ] 不提交、不推送；本次不改构建入口，无需重新构建嵌入包。
