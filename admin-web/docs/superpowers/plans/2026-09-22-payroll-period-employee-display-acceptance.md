# 薪资周期与员工展示验收记录

## 页面恢复后的浏览器复验（2026-09-23）

- 本地 5173 已恢复，登录后的原生薪资页面可交互。此前服务阻塞记录属于历史状态。
- 修复初始化异常：种子数据调用工作周函数时 state 尚未初始化；提前声明并对未建立状态使用历史周日回退。回归测试先失败、后通过，浏览器示例工作区恢复。
- 修复 Regular 与 OT/OT2 重复计算：净打卡工时应扣除已分类的 OT/OT2 后才是 Regular。回归覆盖普通工时、OT、OT2、带薪休息及零下限。月度示例从错误的 239.50h/$5115 恢复到 207.00h/$4465，周视图和日期列表一致。未点击实际数据的确认保存。
- 七种示例按钮逐一点击通过；一月显示五周；双周转半月 2027/07/01 为第15期；4天过渡为 09/01–09/04。完整工作周弹框能打开。
- 设置入口、历史规则、独立新增弹框、半月字段联动及取消关闭通过；未测试确认发布，真实模式仍只读。
- 批量导出弹框含三个示例员工、精简/详细、PDF/CSV、合并/ZIP及开始导出按钮；未执行文件生成，不能记为导出验收通过。
- 已退出隔离示例，恢复实际门店/员工。未验证所有缓存/API隔离路径。
- 新发现：现存实际数据 2026/01/04–01/17 仍标为第1期；需要区分历史锁定记录与可调整记录的迁移方案，不可直接覆盖历史期号。
- 工作周、Regular去重、历史期次保留、薪资状态/领域、七场景生成、批量导出结构检查及 tsc 通过。此前预览修复的完整构建已确认退出0；该构建早于本次薪资修复，不代表本次产物已发布。
- 当前结论：部分通过，尚非完整验收。正式规则发布/真实迁移、实际导出文件、历史期号迁移和其他明细流程仍待完成。无提交或推送。

## 原生规则设置演示接入（2026-09-23）

- 原生薪资 Store/Year/Period 筛选旁新增“发薪周期设置”入口，挂载与销毁跟随原生薪资页。
- 设置支持门店筛选、当前规则摘要、全部历史列表，新增／编辑／查看使用独立弹框，确认／删除需要二次确认。
- 频率联动完整周期起始日、自定义天数和计划日期字段；半月固定发薪日期分为上下半月两行。编辑门店锁定，已生效／过期只读。
- **当前写操作仅限隔离演示**。演示规则只在 controller 内存中保存，不写 API 或 localStorage，不改变薪资 Period。真实数据入口明确只读、禁用新增。不能描述为正式发布已完成。
- 演示历史包括三种过期频率和当前双周示例，按本机日期派生；这是虚构演示，不是门店真实规则历史。正式状态仍需门店时区及真实迁移结果。
- TypeScript 检查、控制器打包和新增 UI 结构验证通过；结构检查不替代浏览器 E2E。真实发布集成、跨期金额归属和完整迁移仍未交付。

## 2026-09-23 复验

结论：逻辑／源码结构验证通过，整体验收仍阻塞，不可发布。

本轮重新运行通过：版本并发、历史期次保护、工作周与折叠契约、批量导出 UI 结构、周期引擎、规则历史生命周期、历史锁定、七场景生成、既有薪资状态、既有薪资领域、批量导出领域，共 11 项脚本。UI 结构测试不等同于实际点击验证。

环境证据：初始 HTTP 连接失败；重新执行项目 dev 命令后 Vite 报 ready，但再次请求首页 5 秒超时。浏览器现有标签为“无法访问此站点”，错误页不可由当前浏览器工具读取，未绕过工具限制。未完成页面交互、真实导出文件、示例退出恢复或生产构建验收。

仍缺规则设置页、完整规则发布集成与真实数据迁移。测试通过仅覆盖已存在模块，不表示这些未完成流程通过验收。本轮仅验收和记录，无业务代码改动、提交或推送。

## 补齐进度

### 保存与历史保护补齐

- `migratePeriods` 不再以固定预设替换非空历史期次；历史 ID、日期、期号、导出元数据及新频率期次保留。失败测试已复现覆盖，修复后验证通过。
- 本地 Mock `/state` 读取返回 revision；保存检查 If-Match，旧版本返回 409、已有版本但缺少条件返回 428；首次空数据库兼容初始化。保存以临时文件加 rename 写入。并发测试通过。
- 原生运行时客户端串行保存并携带 revision；独立 TypeScript repository 同样带 revision，冲突不会降级为本地保存成功。自动同步失败显示明确提示。
- 该原子检查保证单个 Mock 服务进程内的请求不交错，不等同于多实例数据库事务或生产权限校验；不可将两套服务同时指向同一个文件充当生产后端。
- 新增纯函数历史锁定检查，阻止已确认／正式导出／已发薪及日期不明确的期次重新分期；测试通过。尚待最终规则发布操作调用。
- 本轮类型检查、已有薪资状态与领域回归通过。发布接口的规则合法性校验、设置页面、完整迁移与浏览器 E2E 仍未完成；不标记完整验收通过。

- 已实现严格日历日期处理和独立周期引擎（四种预设、自定义、相位／短过渡、计划日期、年度编号）。引擎与历史生命周期测试通过。
- 已实现规则历史派生状态、待生效规则编辑／撤回的纯函数校验；尚未接入发布接口和设置页。
- 工作周分组优先读取期次 `workweekStartDay`，其次读取 `data.workweekSettings` 的门店配置；历史缺省仍保留周日。主表、详情、导出、完整周核对共用该选择逻辑。需要后续迁移／接口提供真实门店配置，不能凭门店名称推断。
- 已加入每周展开／收起及全部展开／收起，日期列表隐藏批量折叠按钮。相关源码契约及周一／周日边界测试通过；浏览器交互仍待验证。
- 服务排查：同一进程启动 Vite 后请求资源返回 200；跨工具调用的后台服务仍超时，尚未确认根因，不能标记已恢复。
- 本轮不提交、不推送。规则发布、真实数据迁移、服务端锁定与端到端验收仍未完成。

日期：2026-09-22

## 结论

未通过完整验收。逻辑测试通过；构建与实际页面交互仍未完成验证，不能据此发布。

## 已通过

- TypeScript `tsc --noEmit` 成功退出。
- `verify-payroll-schedule-demo.ts`：七场景 2027 年期次连续、期末年度归属正确；每期三位虚构员工及仅小费员工存在。
- 2026/01/04 开始的双周期次为第 2 期。
- 双周转半月场景 2027/07/01 开始的期次为第 15 期。
- 四天过渡场景为 2027/09/01–09/04；月度覆盖 2028/02/29。
- 两次生成的示例对象互不污染。
- 既有薪资领域、状态、批量导出领域验证脚本通过。测试使用本地 esbuild 打包运行，无新增依赖。

## 阻塞和待验收

- 实际入口 `http://127.0.0.1:5174/#/team/payroll-report` 的 HTTP 请求在 8 秒超时；浏览器读取页面也超时。
- 之前启动的完整构建仍未返回退出结果，因此构建不能标为通过。
- 七场景按钮、日期／工作周视角切换金额一致性、完整周核对弹框、详情与导出全日期覆盖、退出演示恢复实际数据，均需在页面恢复后验收。
- 生成器对象隔离测试不等于运行时 API／缓存隔离验收；后者仍待实际交互验证。
- 真实规则版本发布与服务端核算接入不在本次已完成范围，详见主实施计划。

本轮仅进行验收与记录，没有提交、合并或推送。

## 完整验收复核

- 重新启动开发服务成功（Vite ready），但 `/`、`/package.json`、`/@vite/client` 和 `/index.html` 请求仍超时；浏览器 `Page.navigate` 也超时。TCP 已建立，不足以认定 HTTP 服务正常。根因尚未确认。
- 旧完整构建会话已失效，未取得成功退出证据；不再沿用“构建仍在运行”的描述。
- `verify-payroll-batch-export-ui.mjs` 通过；它是源码结构断言，不是浏览器 E2E。
- 已确认开发缺口：当前薪资页未接入完整规则设置／新增／发布与历史管理流程。现有实现为演示场景接入，不能代替规则引擎验收。
- 已确认展示缺口：`resolveWeekIndex` 与 `getWeekRangeTextFromPeriod` 固定按周日分组，尚未读取门店实际工作周定义；`renderManageWeekTitleHtml` 只输出标题与完整周按钮，尚未提供已约定的展开／收起操作。
- 本轮未修改业务代码。完整验收结论仍为不通过：需补齐上述实现并恢复服务后，才可验证全部端到端路径。
