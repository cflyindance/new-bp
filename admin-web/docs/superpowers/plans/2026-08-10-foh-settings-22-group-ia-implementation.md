# 前厅设置 22 组信息架构实施计划

## 目标

按已批准规格将前厅设置调整为复杂版 22 组/154 项、MVP 20 组/119 项；只修改分组、名称、顺序、兼容路径和退役目录，不改变保留设置的交互、存储或产线集合。

## 实施步骤

1. 建立回归护栏
   - 新增专项校验脚本，固化 22 组完整顺序、154 个 seq、MVP 可见集合和旧路径目标。
   - 固化迁移前保留项的产线与存储映射基线。
2. 收敛分组 SSOT
   - 修改 `scripts/lib/foh-settings-groups.mjs` 的组顺序、标题、有序 seq 和别名重定向。
   - 让 `scripts/lib/settings-intra-group-sort.mjs` 直接消费前厅有序 seq SSOT。
3. 同步运行时
   - 修改 `src/config/foh-settings-group-keys.ts` 的镜像、规范化与路径重定向。
   - 删除 `src/main.ts` 内重复的旧路径表，统一调用共享规范化函数。
   - 将 seq 164 加入全版本退役，并将 164/176/177 加入 catalog 排除。
4. 生成派生产物
   - 同步前厅产线提取、seed、scope、存储注册、catalog、平台预设与当前设计文档。
   - 更新订单中心生成器和当前文档，不再把 seq 164 计作有效项。
5. 验证
   - 运行前厅 22 组专项校验、产线 scope、产线存储、平台预设 L3、TypeScript 和生产构建。
   - 检查差异，只保留本方案及工作区原有任务的预期变更。

## 安全边界

- 不覆盖或回退工作区现有未提交改动。
- 生成前后比较保留 seq 的 `lines` 与存储映射；任何非允许字段变化均视为失败。
- 不提交、不推送，除非用户另行要求。
