# 前厅管理中心 · 设置二级导航重设计方案

> 文档版本：v4.0（方案 A：员工端 11 组 + 食客端 11 组，共 22 组）
> 数据范围：复杂版本前厅设置 catalog 154 项；MVP 20 组、119 项
> 竞品参考：Toast / Clover / Square / Peblla / Snackpass 商家后台结构文档及《餐饮行业竞品后台信息架构深度分析》

---

## 1. 背景与目标

### 1.1 现状问题

| 指标 | 现状 | 问题 |
|------|------|------|
| 二级分组数 | 曾达 **24+ 组** | 组过多、单组条目过多，侧栏难扫读 |
| 命名来源 | 多为旧系统「功能模块」 | 如「常见按键」「其他按键」，商户难以理解 |
| 重复组名 | 「展示设置」「菜单」等重复出现 | 无法区分 POS 与 eMenu |
| 渠道不清 | POS / iPad / eMenu / 客显屏混排 | 不符合竞品「场景 + 渠道」心智 |

### 1.2 设计目标

- 二级导航收敛为 **22 个** 餐饮场景分类，员工端与食客端各 11 组
- 每条功能设置有明确归属与归类理由
- 为 `docs/项目文档/配置归类-分组映射.csv` 提供可执行的 `groupTitle` / `groupKey`
- **不修改** `配置归类-终版.md` 原文

---

## 2. 竞品对照（前厅/前台设置维度）

| 竞品 | 前厅/前台设置组织方式 | 本项目借鉴 |
|------|----------------------|------------|
| **Toast** | 独立「前厅」：点餐界面设置、快速点餐、桌边服务、服务区与餐位、移动点餐 | 桌台独立成组；操作按钮显隐、工具栏与菜单布局分轨 |
| **Snackpass** | 设置→前台：全渠道 / 在线 / Kiosk与收银台；小费单独页 | 食客端规则与菜单分轨；终端用标签表达渠道 |
| **Square** | 履行方式分 POS/线上；设备模式 | 组内用终端列标注 scope |
| **Peblla** | 设置中心 + 表单「适用场景/渠道」 | 组内用终端列标注 scope |
| **Clover** | 前厅弱，多在设置→交易 | 订单类型/折扣等留在订单/支付中心 |

### 2.1 前厅设置五维（商户心智）

```text
桌台与餐位 → POS 收银点单 → 食客端扫码点餐 → 客显屏
```

---

## 3. 推荐二级导航结构（22 组，方案 A，已确认）

| 序号 | groupTitle | groupKey | 条数 | 说明 |
|------|------------|----------|------|------|
| 1 | **登录与终端主界面** | `foh-pos-shell` | 5 | 员工登录 POS 后的默认终端主界面、会话规则与主页密码门禁。 |
| 2 | **选桌与开台流程** | `foh-table-start-flow` | 7 | 选桌页、人数页、就餐人数规则与开单前换桌/一桌多单；不含清桌与企台。 |
| 3 | **POS 菜单与界面** | `foh-pos-menu-scope` | 10 | 收银端菜单搜索、比价、时段菜单，以及组/类/菜布局和界面展示。 |
| 4 | **点单内容与客户信息** | `foh-pos-order-cart` | 11 | 点单内容展示、减菜交互、只读菜与送厨后调味权限，以及 POS 客户姓名/电话必填；不含套餐点单。 |
| 5 | **套餐与自定义点单** | `foh-pos-combo-ordering` | 3 | 自定义手写录菜、自动点完套餐与可调套餐子菜价格展示（139/145 按产线）。 |
| 6 | **点单页按钮显隐** | `foh-pos-buttons` | 22 | 点单页操作按钮是否收入「更多」（193–215）。 |
| 7 | **点单页工具栏** | `foh-pos-order-toolbar` | 5 | 点单页四区工具栏按钮排序/显隐，以及自定义分割线名称（483–486、196）。 |
| 8 | **送厨规则与权限** | `foh-kitchen-send-timing` | 6 | 收银侧送厨规则与权限：延迟参数 → 手动送厨 → 打单/付款/结账自动送厨 → iPad 送厨密码；不含后厨出票规则。 |
| 9 | **找单与结账入口** | `foh-pos-find-order-list` | 6 | POS/PayPad 找单列表默认筛选、汇总展示、批量关单、打印收据，以及条码找单进付款和支付前客户确认。 |
| 10 | **消息类型提醒** | `foh-pos-order-alerts` | 6 | POS/员工端通知类型、新单语音播报，以及扫码/线上下单等渠道的店内员工订单消息；新单、追单、指定菜品按产线配置。 |
| 11 | **清桌与换服务员（企台）** | `foh-table-clear-ops` | 4 | 付款后按产线延时自动清桌、手动清桌、iPad 清桌通知与更换企台；餐位平面图 seq 428 为独立功能页。 |
| 12 | **订单类型、取餐与送厨** | `foh-guest-order-type` | 8 | 堂吃/外带/来取范围、订单类型与取餐方式页、号码牌、送餐到桌餐牌号，以及食客端送厨规则。 |
| 13 | **食客登记与会员** | `foh-guest-registration` | 7 | 点单前会员身份、短信验证码，及手机号/姓名登记与隐私条款默认勾选。 |
| 14 | **点单前限制与授权** | `foh-guest-pre-order` | 3 | 限制提前开单、下单前服务员授权、切换品类授权。 |
| 15 | **食客端语言** | `foh-guest-facing-locale` | 2 | 食客端（eMenu/Kiosk/客显）可选语言列表与默认语言（652/653）。 |
| 16 | **点餐首页与入口** | `foh-guest-menu-home` | 7 | 菜单组、版式框架（eMenu Pro）、品类/分类浏览结构、纯展示、开始按钮与品牌露出。 |
| 17 | **菜单与购物车展示** | `foh-guest-menu-body` | 18 | 菜单页视觉（类名/图片/详情/字号）、积分露出、菜单树导航/瀑布流，及购物车展示。 |
| 18 | **火锅点餐** | `foh-guest-hotpot` | 4 | 锅底必选/下单方式/加收与下单后仍展示；无火锅业态可跳过。 |
| 19 | **计时与自助餐规则** | `foh-guest-duration-scenarios` | 7 | 按时计价场景、自助餐品类先下单与用餐时长展示/提示/禁下单；无 KTV/自助餐/限时用餐可跳过。 |
| 20 | **桌边呼叫** | `foh-tableside-service` | 3 | 桌边呼叫服务员及服务类型。 |
| 21 | **点单备注** | `foh-guest-order-notes` | 3 | 订单、商品与套餐子项备注开关。 |
| 22 | **等待时长计算与展示** | `foh-wait-time-display` | 7 | Kiosk / Online Order 点单页预计等待时长展示、区间规则与样式；主开关 + 产线多选。 |
| | **合计** | | **154** | |

### 3.0 v4.0 变更说明（22 组信息架构收敛，已确认）

在保留员工端/食客端各 11 组的前提下，合并过细分类并修正边界：

| 调整 | 目标组 | seq |
|------|--------|-----|
| 菜单查找与布局合并 | `foh-pos-menu-scope` | 118、174、148、348、216–220、350 |
| 分割线/结账入口合并 | `foh-pos-order-toolbar` / `foh-pos-find-order-list` | 196；248、221 |
| 食客端送厨并入履约 | `foh-guest-order-type` | 581、502 |
| 桌边服务拆出点单备注 | `foh-guest-order-notes` | 521–523 |

seq 164、176、177 从当前目录排除；保留设置的交互、存储和产线集合不变。

以下小节为历史演进记录；当前结构以 §3 主表与本节为准。

### 3.0.1 v3.4 变更说明（侧栏员工端/食客端视觉分隔，已确认）

前厅设置二级侧栏在 **清桌与企台** 与 **订单类型与取餐** 之间插入分段：

- **员工端**（11 组）：`foh-pos-shell` … `foh-table-clear-ops`
- **食客端**（11 组）：`foh-guest-order-type` … `foh-wait-time-display`

catalog 字段 `groupNavSections`（`labelKey` + `groupKeys`）；UI 见 `renderModuleSettingsSubnavList`。

### 3.0.2 v3.3 变更说明（侧栏动线重排，方案 A，已确认）

20 组按 **员工端完整一单 → 食客端完整旅程** 重排侧栏（仅调整 `groupOrder`，不改 groupKey/归属）：

**A 员工端（11）**：登录与主界面 → 选桌与开台流程 → 菜单查找与时段 → 菜单区界面布局 → 订单行与客户信息 → 套餐与自定义点单 → POS 操作按钮 → 送厨时机 → POS 找单列表 → POS 结账入口 → 清桌与企台

**B 食客端（11）**：订单类型与取餐 → 食客登记与会员 → 点单前须知与授权 → 食客端语言 → 点餐首页与入口 → 菜单展示与购物车 → 食客端送厨 → 火锅点餐 → 时长与自助餐 → 桌边服务 → 排队与等待展示

相对 v3.2 挪动：**登录与主界面** 置顶；**清桌与企台** 移至结账入口之后；**食客端语言** 提前至菜单展示之前。

### 3.0.3 v3.2 变更说明（桌台与开台拆 2 组，已确认）

原 **`foh-tables-start`（桌台与开台，13 条）** 拆为：

| 新组 | groupKey | 条数 | seq |
|------|----------|------|-----|
| 选桌与开台流程 | `foh-table-start-flow` | 7 | 107、619、111、625、621、643、592 |
| 清桌与企台 | `foh-table-clear-ops` | 4 | 534、642、351、347 |

侧栏顺序：**选桌与开台流程 → 清桌与企台** → 登录与主界面 → …

书签：`foh-tables-start` / `tables-floor` / `pos-order-init` → `foh-table-start-flow`；`table-clear-ops` → `foh-table-clear-ops`。

### 3.0.4 v3.1 变更说明（POS 菜单布局拆 2 组，已确认）

原 **`foh-pos-menu-layout`（POS 菜单布局，11 条）** 拆为：

| 新组 | groupKey | 条数 | seq |
|------|----------|------|-----|
| 菜单查找与时段 | `foh-pos-menu-scope` | 5 | 118、148、176、177、348 |
| 菜单区界面布局 | `foh-pos-menu-ui-layout` | 6 | 216–218、220、219、350 |

侧栏顺序：… → 登录与主界面 → **菜单查找与时段 → 菜单区界面布局** → 订单行与客户信息 → …

书签：`foh-pos-menu-layout` / `pos-menu-ui` → `foh-pos-menu-scope`；`pos-menu-ui-layout` → `foh-pos-menu-ui-layout`。

### 3.0.5 v1.25 变更说明（默认主界面迁前厅，已确认）

自系统设置 `ui-operation-preferences` 迁入 **`pos-shell-landing`（主界面与导航）**：

| seq | 功能设置 | 说明 |
|-----|----------|------|
| 165 | 默认主界面 | 员工登录 POS / POS GO / PayPad 后的默认壳层页（MAIN / TABLE / ORDER / RECALL）；**门店 × 产线兜底**；UI：`module-settings-default-main-screen-ui.ts` |

系统设置 **界面与操作偏好** 仅保留 168（24 小时制）、174（菜单模式）。侧栏置于 `tables-floor` 之后。

### 3.1 v1.1 变更说明（已确认）

将原 **POS 操作与按钮**（`pos-actions`，31 条）拆为两组，便于侧栏扫读与对标竞品「按钮显隐」与「工具栏布局」分轨：

| 新组 | groupKey | 条数 | 范围 |
|------|----------|------|------|
| 操作按钮显隐 | `pos-button-visibility` | 26 | seq 118、164、193–215、196、347 |
| POS 点单页工具栏 | `pos-order-toolbar` | 4 | seq 483–486（整单/菜品详情/订单信息/订单金额） |

catalog 对 `/operations/queue-call/settings` 使用固定 `groupOrder`，保证侧栏顺序为：桌台 → **主界面与导航** → 开单 → 送厨 → 按钮 → 工具栏 → 点单页 → 找单列表 → **结账入口** → 菜单查找与时段 → 菜单区界面布局 → …

### 3.2 v1.17 变更说明（结账入口迁前厅，已确认）

自订单中心 `checkout-entry` 迁入 **`pos-checkout-entry`（POS 结账入口）**：

| seq | 功能设置 |
|-----|----------|
| 248 | 当用条形码找单时打开单子付款界面 |
| 221 | 支付前确认客户信息 |

侧栏置于 `pos-find-order-list` 之后；订单中心不再保留结账入口组。

### 3.2.1 v1.19 变更说明（订单类型范围 + Kiosk 履约流程迁前厅，已确认）

自外卖/来取迁入 Kiosk 履约流程至 **`guest-order-rules`**：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 487 | 订单类型可用范围（堂吃/外带/来取） | 前厅 · `guest-order-rules` |
| 488 | 展示订单类型选择页面 | 前厅 · `guest-order-rules` |
| 489 | 送餐取餐服务方式（多选） | 前厅 · `guest-order-rules` |
| 490 | 展示取餐方式 | 前厅 · `guest-order-rules` |
| 491 | 打包展示输入号码牌 | 前厅 · `guest-order-rules` |

外卖/来取仅保留线上下单基础与外送打包；订单中心保留默认新订单类型（126）。

### 3.3 v1.16 变更说明（找单列表迁前厅，已确认）

自订单中心 `find-order-list` 迁入 **`pos-find-order-list`（POS 找单列表）**：

| seq | 功能设置 |
|-----|----------|
| 151 | 显示所有单的总计价格 |
| 152 | 显示「关闭以下全部单子」按钮 |
| 153 | 默认显示未加小费订单 |
| 251 | 找单界面打印所选收据类型 |

（seq 154「盘点模式」已下线，不再展示。）

与 `pos-order-cart`（点单购物车）分轨。

### 3.4 v1.15 变更说明（点单界面迁前厅，已确认）

自订单中心移除 **`order-ui`**，新增 **`pos-order-cart`（点单页展示）**：

| seq | 功能设置 | 说明 |
|-----|----------|------|
| 121 | 订单数量支持小数 | 行项数量 |
| 122 | 减菜后自动重定向 | 点单交互 |
| 132 | 点单显示座位 | 按订单类型 |
| 133 | 相同菜品展示 | 与 134 合并单选（拆分/合并） |
| 135–137 | 菜序/键盘/ASAP | 点单页展示 |
| 178 | 显示单菜序号 | 单菜设置 |

与 **`foh-pos-menu-scope` / `foh-pos-menu-ui-layout`**（菜单查找与布局）分工：本组管购物车行项，彼两组管菜单区。

### 3.3 v1.14 变更说明（送厨策略迁前厅，已确认）

自订单中心移除 **`kitchen-send`**，迁入 **`pos-kitchen-send`**：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 113 | 点击「送厨」整单送厨 | 前厅 · `pos-kitchen-send` |
| 114 | 点击「付款」直接送厨 | 前厅 · `pos-kitchen-send` |
| 120 | 结账后自动送厨 | 前厅 · `pos-kitchen-send` |
| 123 | 打单后自动送厨 | 前厅 · `pos-kitchen-send` |
| 125 | 延迟送厨时间 | 前厅 · `pos-kitchen-send` |

订单中心现为 **6 组**（无 `kitchen-send`）；与后厨 **62/304**、按钮 **198/483** 分工见订单/后厨文档。

### 3.4 v1.13 变更说明（开单·桌台部分迁前厅，已确认）

自订单中心 `order-init-scenario` 迁入桌台/人数开单项；**126 默认新订单类型** 留订单中心：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 107 | 跳过选桌 | 前厅 · `tables-floor` |
| 619 | 人数选择 | 前厅 · `tables-floor` |
| 643 | 开单前，换桌 | 前厅 · `tables-floor` |
| 592 | 不允许一桌多单 | 前厅 · `tables-floor` |
| 108 | 跳过选择人数 | 前厅 · `pos-order-init` |
| 111 | 每单最多客人数量 | 前厅 · `pos-order-init` |
| 625 | 儿童不参与人数计算 | 前厅 · `pos-order-init` |

订单中心 `order-init-scenario` 仅保留 **126**（1 条）。

### 3.3 v1.12 变更说明（开单·桌台与场景方案 B，已确认）

自订单中心 `order-init-scenario` 迁入场景与桌台/POS 相关项：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 571 | 品类先下单 | 前厅 · `guest-order-rules` |
| 572 | 火锅锅底必选 | 前厅 · `guest-order-rules` |
| 574 | 火锅锅底下单方式 | 前厅 · `guest-order-rules` |
| 575 | 同一锅型锅底过半加收 | 前厅 · `guest-order-rules` |
| 533 | 选择桌子页面 | 前厅 · `tables-floor` |
| 110 | 点单超时提醒(分钟) | 前厅 · `pos-order-toolbar` |

订单中心 `order-init-scenario` 仅保留 POS 通用开单 **107/108/111/126/619/625/643/592**（8 条；v1.13 再迁 7 条，仅留 126）；**593** 归商品管理（catalog 不展示）。

### 3.4 v1.11 变更说明（送厨策略方案 B，已确认）

历史曾将 **567 菜单延迟送厨** 迁入 `guest-order-rules`；现与 **125 延迟送厨时间**（`pos-kitchen-send`）合并，567 不再在设置滑层展示。

| seq | 功能设置 | 迁入 |
|-----|----------|------|

订单中心 `kitchen-send` 仅保留 POS/结账侧 **113/114/120/123/125**（5 条；**v1.14 已全部迁前厅 `pos-kitchen-send`**）。

### 3.5 v1.10 变更说明（送厨策略方案 A，已确认）

自订单中心 `kitchen-send` 迁入 C 端/Kiosk 送厨规则至 **`guest-order-rules`**：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 502 | 可自动送厨的订单支付状态 | 前厅 · `guest-order-rules` |
| 581 | 菜单送厨方式 | 前厅 · `guest-order-rules` |

订单中心 `kitchen-send` 保留 POS/结账侧 **113/114/120/123/125**（5 条；567 在 v1.11 迁出；**v1.14 已全部迁前厅 `pos-kitchen-send`**）。

### 3.6 v1.9 变更说明（已确认）

收紧 **操作按钮显隐** 边界，仅保留 seq **193–215**（是否收入「更多」）；其余迁出后，`148` 已在 v1.8 归位前厅；v1.9 将 `655–661` 自商品中心迁入前厅 `guest-menu-scenarios`：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 118 | 搜索菜单 | 前厅 · `pos-menu-ui` |
| 196 | 自定义分割线名称 | 前厅 · `pos-order-toolbar` |
| 347 | 允许更换企台 | 前厅 · `tables-floor` |
| 164 | 自定义折扣原因 | 订单中心 · `discount-void`（与 162/163 同组） |
| 148 | 比价功能模式 | 前厅 · `pos-menu-ui`（v1.8） |
| 443 | 按时计价 | 前厅 · `guest-order-rules`（v1.8） |
| 655–661 | 品类设置/分类设置（自助餐与场景菜单） | 前厅 · `guest-menu-scenarios`（v1.9） |

设置 catalog 通过 `scripts/lib/settings-hub-override.mjs` 挂载跨 hub 项（**不修改**终版原文）。

### 3.7 POS 菜单拆组后组内排序（已确认）

- **`foh-pos-menu-scope`**：118 搜索 → 148 比价 → 176/177 时段 → 348 iPad 时段
- **`foh-pos-menu-ui-layout`**：216 组 → 217 类 → 218 菜 → 220 价格 → 219 按钮样式 → 350 iPad 消息

### 3.8 客显屏边界收紧（已确认）

结账交互迁支付中心；**界面语言**迁 §3.14；客显屏组内封面/Logo/场景见 §3.15：

| seq | 功能设置 | 迁入 |
|-----|----------|------|
| 463 | 小费（客显屏展示小费页） | 支付中心 · `cds-checkout-ux` |
| 465 | 小票（客显屏自主选择打印） | 支付中心 · `cds-checkout-ux` |
| 467 | 语言设置 | catalog 不展示（SSOT 652/653，见 §3.14） |

### 3.9 备注项去重（已确认）

从 `guest-notes-fees` **移除** seq **609、610**（全局「展示菜品/订单备注」），与 **522 商品备注、521 订单备注** 场景重复；catalog 不展示 609/610。

### 3.10 备注组边界收紧（已确认）

seq **616、617、618**（购物车送厨状态/价格/售罄隐藏）自备注组迁入食客端菜单（v1.3 为 **`guest-menu-cart`**）；`guest-notes-fees` 仅保留备注 521–523 与附加费 544、545。

### 3.11 下单与规则边界收紧（已确认）

seq **600**（全局纯展示模式，不可加购）自下单规则迁入食客端菜单（v1.3 为 **`guest-menu-global`**）；v1.8 将 `443` 并入 `guest-order-rules`；v1.10/v1.11 送厨迁入；**v1.12** 并入火锅/品类 **571–575**，当前组内 18 条。

| 配置 | seq | 说明 |
|------|-----|------|
| 全局纯展示 | 600 | 前厅 · `guest-menu-global`（v1.3） |
| 单桌/设备纯展示 | 561 | 硬件管理中心（覆盖关系见硬件方案） |

### 3.12 菜单展示去重与硬件分工（已确认）

**catalog 去重**（保留服务设置 SSOT，不展示全局重复项）：

| 保留（SSOT） | 排除（catalog 不展示） |
|--------------|------------------------|
| 515 展示菜单序号 | 614 展示菜品编号 |
| 516 显示组名称 | 605 展示组名称 |
| 524 瀑布流模式 | 603 瀑布流模式 |
| 528 菜价为0展示价格 | 613 菜价为0,展示价格 |

**全局 vs 硬件双入口**（均保留在前厅 catalog，文档约定覆盖关系）：

| 能力 | 前厅 seq | 硬件 seq |
|------|----------|----------|
| 默认展示菜单组 | 599 | 560 |
| 纯展示模式 | 600 | 561 |
| eMenu Pro 版式 | 604 | 569 |

### 3.13 食客端菜单展示拆组（v1.3，已确认）

原 **`guest-menu`**（23 条）侧栏单组过长，并在 v1.9 吸收商品中心自助餐场景菜单（655–661），现按「菜单结构 / 品类与场景菜单 / 全局展示 / 购物车」拆为 4 个二级导航：

| 新组 | groupKey | 条数 | seq |
|------|----------|------|-----|
| 食客端·菜单结构 | `guest-menu-structure` | 8 | 515–520、524、528 |
| 食客端·品类与场景菜单 | `guest-menu-scenarios` | 7 | 655–661 |
| 食客端·首页与版式 | `guest-menu-global` | 11 | 532、599–611、600、604、645 |
| 食客端·购物车展示 | `guest-menu-cart` | 3 | 616–618 |

侧栏顺序：… → POS 菜单与布局 → **菜单结构 → 品类与场景菜单 → 首页与版式 → 购物车** → 界面语言 → 下单与规则 → …

### 3.14 对客界面语言（v1.4，已确认）

**问题**：467 挂在客显屏，但 eMenu / Kiosk 等同属 C 端也需配置界面语言；513/514 与 652/653 为重复入口。

| 能力 | catalog SSOT | 排除（不展示） | 生效渠道 |
|------|--------------|----------------|----------|
| 可选语言列表 | 652 | 513、467 | eMenu、Kiosk、客显等 C 端 UI |
| 默认语言 | 653 | 514 | 同上 |

- **挂载**：652/653 终版归属系统设置，经 `settings-hub-override.mjs` 在前厅 **`guest-facing-locale`** 展示（不改终版原文）。
- **分工**：**109** 留在系统设置（店员 POS 默认语言）；**456** 在商品中心（菜单文案多语言）。

### 3.15 C 端视觉与客显分工（v1.5–v1.7，已确认）

侧栏为单一 **「客显屏」**（`cds`，3 条）；组内顺序：461 封面 → 462 Logo → 466 场景启用。

| 能力 | 入口 hub | seq | 说明 |
|------|----------|-----|------|
| 客显屏封面、Logo | 前厅 · `cds` | 461、462 | 客显专用；滑层注明「维护请至素材中心」 |
| Pickup/Delivery 启用客显 | 前厅 · `cds` | 466 | 前场渠道规则 |
| Kiosk/eMenu 首页封面、门店 Logo | **营销中心 / 门店管理** | 555、556；433 餐厅 LOGO → 门店档案 | 默认展示，无显隐开关 |
| eMenu/Kiosk MenuSifu 品牌露出 | 前厅 · `guest-menu-global` | 532 | 是否展示（显隐） |
| 屏显图片库 | **素材中心** | 431 | 图片库维护 |

---

## 4. 分类结果明细

### 4.1 登录与终端主界面（`foh-pos-shell`）

**归类理由**：员工登录 POS 后的默认终端主界面、会话规则与主页密码门禁。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 75 | 前厅 | 操作管理 | 系统安全 | 自动登出时间(分钟) | 设置超过多久系统没有操作，自动退出登录 |
| 166 | 前厅 | 其他 | 基本设置 | 完成每次操作后账号自动登出 | 设置POS系统完成每次操作后账号是否自动登出 |
| 175 | 前厅 | 其他 | 基本设置 | 登录忽略特殊符号 | 设置进行登录输入时是否允许输入特殊符号 |
| 165 | 前厅 | 其他 | 基本设置 | 默认主界面 | 门店兜底：员工登录 POS / POS GO / PayPad 后的默认主界面（MAIN / TABLE / ORDER / RECALL）；各产线独立配置。 |
| 346 | iPad/POS | iPad | 基础设置 | 主页密码权限 | 设置Ipad上使用POS，进入主页是否需要权限 |

### 4.2 选桌与开台流程（`foh-table-start-flow`）

**归类理由**：选桌页、人数页、就餐人数规则与开单前换桌/一桌多单；不含清桌与企台。


**组内展示顺序**：107 选桌页 → 619 人数页 → 111/625/621 人数规则 → 643 开单前换桌三态矩阵 → 592 一桌多单。

**107/533** 为选桌 SSOT；**108/619** 为人数页 SSOT；**126** 默认单型留订单中心。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 107 | POS | 点单 | 基础设置 | 跳过选桌 | 设置点单是否需要展示选择桌子页面 |
| 619 | eMenu | 全局设置 | 用户设置 | 人数选择 | 开启后在所选产线展示人数选择页；关闭则跳过。适用产线：Kiosk、eMenu、POS、POS GO、PayPad、SDI。 |
| 111 | POS | 点单 | 基础设置 | 每单最多客人数量 | 设置每单最多可以选择的客人数量 |
| 625 | eMenu | 全局设置 | 用户设置 | 儿童将不参与下单限制规则的人数计算 | 设置儿童是否参与下单限制规则的人数计算 |
| 621 | eMenu | 全局设置 | 用户设置 | 允许食客更改人数 | 设置是否允许食客更改就餐人数。关闭后需要服务员输入密码更改当前桌人数 |
| 643 | eMenu | 全局设置 | 服务员设置 | 开单前，换桌 | 可按产线设置开单前不启用换桌、可选换桌或必须换桌。适用产线：POS、POS GO、PayPad、eMenu、SDI。 |
| 592 | eMenu | 全局设置 | 下单设置 | 不允许一桌多单 | 设置一个桌子是否允许创建多个订单 |

### 4.3 POS 菜单与界面（`foh-pos-menu-scope`）

**归类理由**：收银端菜单搜索、比价、时段菜单，以及组/类/菜布局和界面展示。


**组内展示顺序**：118 搜索菜单 → 174 菜单模式 → 148 比价 → 348 iPad 时段 → 216 组平铺 → 217 类展示 → 218 菜展示 → 220 显示价格 → 219 按钮颜色满铺 → 350 电子菜单自定义消息。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 118 | POS | 点单 | 基础设置 | 搜索菜单 | 开启后，在选定产线（POS、POS GO、PayPad）点单页支持搜索菜单。 |
| 174 | 前厅 | 其他 | 基本设置 | 菜单模式 | 设置点单获取的菜单是哪一套菜单（POS菜单还是eMenu菜单） |
| 148 | POS | 点单 | 价格 | 比价功能模式 | 开启后，在选定产线（POS、POS GO、PayPad）点单页启用比价功能模式。 |
| 348 | iPad/POS | iPad | 基础设置 | 按照时段显示菜单 | 在 eMenu、Kiosk、POS、POS GO、PayPad、SDI 各产线，可分别多选 Dinein、Delivery、Pick Up、ToGo 作为... |
| 216 | POS | 界面设置 | 点餐界面模式 | 组平铺展示 | 开启后，在选定产线（POS、PayPad）点单页菜单组以平铺方式展示。 |
| 217 | POS | 界面设置 | 点餐界面模式 | 类展示 | 开启后，在选定产线（POS、PayPad）按产线独立配置分类展示布局（Horizontal2 / Horizontal 3 / Vertical 1）。 |
| 218 | POS | 界面设置 | 点餐界面模式 | 菜展示 | 开启后，在选定产线（POS、PayPad）按产线独立配置菜品按钮展示布局（Regular Buttons / Large Buttons）。 |
| 220 | POS | 界面设置 | 点餐界面模式 | 显示菜品价格 | 开启后，在选定产线（POS、PayPad）点单页菜单区展示菜品价格。 |
| 219 | POS | 界面设置 | 点餐界面模式 | 按钮颜色满铺 | 开启后，在选定产线（POS、PayPad）点单页菜单按钮颜色铺满按钮区域。 |
| 350 | iPad/POS | iPad | 基础设置 | 电子菜单自定义消息 | 开启后，在选定产线（POS、PayPad）允许配置电子菜单自定义消息。 |

### 4.4 点单内容与客户信息（`foh-pos-order-cart`）

**归类理由**：点单内容展示、减菜交互、只读菜与送厨后调味权限，以及 POS 客户姓名/电话必填；不含套餐点单。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 132 | POS | 点单 | 点单界面设置 | 点单显示座位 | 设置哪种订单类型(Dinein、Delivery、Pick Up、To Go)的点单，点单显示座位，用于一个订单，多个座位，按照座位点单加菜 |
| 133 | POS | 点单 | 点单界面设置 | 相同菜拆分显示 | 设置相同的菜是分开展示，还是合并展示 |
| 135 | POS | 点单 | 点单界面设置 | 菜序模式 | 设置订单中，相同的类（即菜序）下的商品，合并到一个类下进行展示。 |
| 137 | POS | 点单 | 点单界面设置 | 显示ASAP订单时间 | 设置订单时间是否显示ASAP订单时间 |
| 178 | 前厅 | 其他 | 单菜设置 | 显示单菜序号 | 开启后在所选产线点单页展示单个菜的菜品序号 ID。适用产线：POS、POS GO、PayPad、Kiosk、SDI、Online Order、eMenu。 |
| 121 | POS | 点单 | 基础设置 | 订单数量支持小数 | 设置订单数量是否支持小数展示，比如有半份，三分之一份的 |
| 122 | POS | 点单 | 基础设置 | 减菜后自动重定向 | 开启后在所选产线减菜完成后自动重定向。适用产线：POS、POS GO、PayPad。 |
| 222 | POS | 界面设置 | 点餐界面模式 | 客户姓名必填 | 开启后在所选产线 POS 点单页将客户姓名设为必填。适用产线：POS、POS GO、PayPad。 |
| 223 | POS | 界面设置 | 点餐界面模式 | 客户电话必填 | 开启后在所选产线 POS 点单页将客户电话设为必填。适用产线：POS、POS GO、PayPad。 |
| 349 | iPad/POS | iPad | 基础设置 | 允许企台在电子菜单上点只读菜 | 设置Ipad上使用POS，是否允许企台在电子菜单上点只读菜 |
| 141 | POS | 点单 | 自定义单/套餐 | 支持为已送厨的菜修改调味 | 勾选产线后，已送厨菜品行仍允许修改调味/加料。 |

### 4.5 套餐与自定义点单（`foh-pos-combo-ordering`）

**归类理由**：自定义手写录菜、自动点完套餐与可调套餐子菜价格展示（139/145 按产线）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 138 | POS | 点单 | 自定义单/套餐 | 自定义点单 | 开启后在所选产线支持自定义手动录入菜品。适用产线：POS、POS GO、PayPad。 |
| 139 | POS | 点单 | 自定义单/套餐 | 自动点完套餐 | 开启后，在选定产线（POS、POS GO、PayPad、Kiosk、eMenu、SDI、Online Order）点选套餐时自动完成套餐内必选子项。 |
| 145 | POS | 点单 | 价格 | 当套餐价钱规则是可调时,显示套餐子菜价格 | 开启后，在选定产线（POS、POS GO、PayPad、Kiosk、eMenu、SDI、Online Order）当套餐价格为可调整规则时展示子菜价格。 |

### 4.6 点单页按钮显隐（`foh-pos-buttons`）

**归类理由**：点单页操作按钮是否收入「更多」（193–215）。


**组内展示顺序**：193–195 删单/移单/清桌 → 197–212 常见按键 → 213–215 其他按键。

**193–215** 右侧**开关**（关闭/开启标签，保留功能场景描述；原型 localStorage）。**196** 归 `pos-order-toolbar`。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 193 | POS | 界面设置 | 常见按键 | 将"删单"隐藏到"更多" | 设置POS点单页面，将"删单"按钮隐藏到"更多"中... |
| 194 | POS | 界面设置 | 常见按键 | 将"移单"隐藏到"更多" | 设置POS点单页面，将"移单"隐藏到"更多"中... |
| 195 | POS | 界面设置 | 常见按键 | 将"清桌"隐藏到"更多" | 设置POS点单页面，将"清桌"隐藏到"更多"中... |
| 197 | POS | 界面设置 | 常见按键 | 将"编辑"隐藏到"更多" | 设置POS点单页面，将"编辑"隐藏到"更多"中... |
| 198 | POS | 界面设置 | 常见按键 | 将"送厨"隐藏到"更多" | 设置POS点单页面，将"送厨"隐藏到"更多" |
| 199 | POS | 界面设置 | 常见按键 | 将"付款"隐藏到"更多" | 设置POS点单页面，将"付款"隐藏到"更多" |
| 200 | POS | 界面设置 | 常见按键 | 将"合并"隐藏到"更多" | 设置POS点单页面，将"合并"隐藏到"更多" |
| 201 | POS | 界面设置 | 常见按键 | 将"分单"隐藏到"更多" | 设置POS点单页面，将"分单"隐藏到"更多" |
| 202 | POS | 界面设置 | 常见按键 | 将"打单"隐藏到"更多" | 设置POS点单页面，将"打单"隐藏到"更多" |
| 203 | POS | 界面设置 | 常见按键 | 将"小费"隐藏到"更多" | 设置POS点单页面，将"小费"隐藏到"更多" |
| 204 | POS | 界面设置 | 常见按键 | 将"加收"隐藏到"更多" | 设置POS点单页面，将"加收"隐藏到"更多" |
| 205 | POS | 界面设置 | 常见按键 | 将"复制"隐藏到"更多" | 设置POS点单页面，将"复制"隐藏到"更多" |
| 206 | POS | 界面设置 | 常见按键 | 将"打折"隐藏到"更多" | 设置POS点单页面，将"打折"隐藏到"更多" |
| 207 | POS | 界面设置 | 常见按键 | 将"移菜"隐藏到"更多" | 设置POS点单页面，将"移菜"隐藏到"更多" |
| 208 | POS | 界面设置 | 常见按键 | 将"顾客信息"隐藏到"更多" | 设置POS点单页面，将"顾客信息"隐藏到"更多" |
| 209 | POS | 界面设置 | 常见按键 | 将"退款并删单"隐藏到"更多" | 设置POS点单页面，将"退款并删单"隐藏到"更多" |
| 210 | POS | 界面设置 | 常见按键 | 将"叫号"隐藏到"更多" | 设置POS点单页面，将"叫号"隐藏到"更多" |
| 211 | POS | 界面设置 | 常见按键 | 将"类型"隐藏到"更多" | 设置POS点单页面，将"类型"隐藏到"更多" |
| 212 | POS | 界面设置 | 常见按键 | 将"销号"隐藏到"更多" | 设置POS点单页面，将"销号"隐藏到"更多" |
| 213 | POS | 界面设置 | 其他按键 | 将"催菜"隐藏到"更多" | 设置POS点单页面，将"催菜"隐藏到"更多" |
| 214 | POS | 界面设置 | 其他按键 | 将"企台"隐藏到"更多" | 设置POS点单页面，将"企台"隐藏到"更多" |
| 215 | POS | 界面设置 | 其他按键 | 来取/外送历史订单界面:将"复制"隐藏 | 设置POS点单页面，来取/外送历史订单界面:将"复制"隐藏 |

### 4.7 点单页工具栏（`foh-pos-order-toolbar`）

**归类理由**：点单页四区工具栏按钮排序/显隐，以及自定义分割线名称（483–486、196）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 483 | POS | 点单页配置 | 整单操作 | 分单、删单、保存、退出、付款、送厨、直送、仅付款 | 整单操作相关操作的按钮及排序顺序 |
| 484 | POS | 点单页配置 | 菜品详情 | 分割线、加1、数量、减1、备注、税、调味、改价 | 菜品详情相关操作的按钮及排序顺序 |
| 485 | POS | 点单页配置 | 订单信息 | 排序、客人信息、类型、换桌、企台、客人、整单备注、会员、打单 | 订单信息相关操作的按钮及排序顺序 |
| 486 | POS | 点单页配置 | 订单金额 | 加收、折扣、小费、整单税 | 订单金额相关操作的按钮及排序顺序 |
| 196 | POS | 界面设置 | 常见按键 | 自定义分割线名称 | 设置POS点单页面，自定义分割线名称 |

### 4.8 送厨规则与权限（`foh-kitchen-send-timing`）

**归类理由**：收银侧送厨规则与权限：延迟参数 → 手动送厨 → 打单/付款/结账自动送厨 → iPad 送厨密码；不含后厨出票规则。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 125 | POS | 点单 | 基础设置 | 延迟送厨时间 | 设置订单下单后多久再自动送厨 |
| 113 | POS | 点单 | 基础设置 | 点击"送厨"整单送厨 | 设置点击"送厨"订单是否整单送厨 |
| 123 | POS | 点单 | 基础设置 | 打单后自动送厨 | 设置打单后是否自动送厨 |
| 114 | POS | 点单 | 基础设置 | 点击"付款"直接送厨 | 设置点击"付款"订单是否直接送厨 |
| 120 | POS | 点单 | 基础设置 | 结账后自动送厨 | 设置结账后是否自动送厨 |
| 345 | iPad/POS | iPad | 基础设置 | 送厨密码权限 | 设置Ipad上使用POS，送厨操作是否需要权限 |

### 4.9 找单与结账入口（`foh-pos-find-order-list`）

**归类理由**：POS/PayPad 找单列表默认筛选、汇总展示、批量关单、打印收据，以及条码找单进付款和支付前客户确认。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 153 | POS | 点单 | 找单 | 默认显示未加小费订单 | 勾选产线后，找单列表默认筛选展示未加小费的订单。 |
| 151 | POS | 点单 | 找单 | 显示所有单的总计价格 | 勾选产线后，找单列表页展示所有订单的总价合计。 |
| 152 | POS | 点单 | 找单 | 显示："关闭以下全部单子"按钮 | 勾选产线后，找单列表展示「关闭以下全部单子」操作按钮。 |
| 251 | 前厅 | 支付相关 | 付款收据 | 找单界面打印所选收据类型 | 勾选产线后，找单页打印时可选择收据类型。 |
| 248 | 前厅 | 支付相关 | 付款收据 | 当用条形码找单时打开单子付款界面 | 勾选产线后，扫描条形码找单时直接进入该单付款界面。 |
| 221 | POS | 界面设置 | 点餐界面模式 | 支付前确认客户信息 | 勾选产线后，支付前需服务员二次确认客户信息。 |

### 4.10 消息类型提醒（`foh-pos-order-alerts`）

**归类理由**：POS/员工端通知类型、新单语音播报，以及扫码/线上下单等渠道的店内员工订单消息；新单、追单、指定菜品按产线配置。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 331 | 前厅 | 系统功能 | 消息中心 | 员工端通知类型 | 多选启用哪些员工端通知类型（按订单/服务/运营/交易/系统分类展示）；仅控制 POS/员工端是否接收该类提醒，具体业务配置见对应中心。 |
| 332 | 前厅 | 系统功能 | 消息中心 | 新单语音播报 | 多选适用产线：Kiosk、eMenu、POS、POS GO、PayPad、SDI、Online Order；勾选后该产线新订单进行语音播报提醒。 |
| 638 | eMenu | 全局设置 | 消息通知 | 新订单提醒 | 勾选产线后，该产线有新订单进入店内时向员工发送消息提醒（不发顾客短信）。 |
| 639 | eMenu | 全局设置 | 消息通知 | 追单提醒 | 勾选产线后，该产线在追单/加菜时向员工发送消息提醒（不发顾客短信）。 |
| 637 | eMenu | 全局设置 | 消息通知 | 指定菜品提醒 | 勾选产线后，订单含指定菜品时向员工发送消息提醒；指定菜品列表配置见业务规则（原型待接）。 |
| 110 | POS | 点单 | 基础设置 | 点单超时提醒(分钟) | 设置订单创建后显示警告的时间间隔。 |

### 4.11 清桌与换服务员（企台）（`foh-table-clear-ops`）

**归类理由**：付款后按产线延时自动清桌、手动清桌、iPad 清桌通知与更换企台；餐位平面图 seq 428 为独立功能页。


**组内展示顺序**：534 付款后自动清桌 → 642 展示清桌快捷按钮 → 351 清桌通知 → 347 更换企台；原 169 已合并至 534。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 534 | eMenu | 服务设置 | 桌子设置 | 付款后自动清桌 | 付款完成后，按各产线设置的超时分钟数自动清桌；0 表示该产线不启用。原 seq 169 已合并。 |
| 642 | eMenu | 全局设置 | 服务员设置 | 展示清桌快捷按钮 | 设置是否展示清桌快捷按钮（eMenu） |
| 351 | iPad/POS | iPad | 基础设置 | 启用向客户端发送清桌通知 | 设置Ipad上使用POS，是否启用向客户端发送清桌通知 |
| 347 | iPad/POS | iPad | 基础设置 | 允许更换企台 | 设置Ipad上使用POS，是否允许更换服务员 |

### 4.12 订单类型、取餐与送厨（`foh-guest-order-type`）

**归类理由**：堂吃/外带/来取范围、订单类型与取餐方式页、号码牌、送餐到桌餐牌号，以及食客端送厨规则。


**组内展示顺序**：487–491 订单类型与取餐流程 → 503 送餐到桌餐牌号 → 581 菜单送厨方式 → 502 可自动送厨的支付状态。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 487 | eMenu | 服务设置 | 订单类型 | 订单类型可用范围（堂吃/外带/来取） | 设置订单类型是支持堂吃，还是支持打包，还是支持外带 |
| 488 | eMenu | 服务设置 | 订单类型 | 展示订单类型选择页面 | 设置食客端是否展示订单类型选择页面 |
| 489 | eMenu | 服务设置 | 订单类型 | 送餐取餐服务方式 | 设置食客端是否展示送餐取餐服务方式（柜台自取/服务员送餐到桌） |
| 490 | eMenu | 服务设置 | 订单类型 | 展示取餐方式 | 设置食客端是否展示送餐取餐服务方式选择页面 |
| 491 | eMenu | 服务设置 | 订单类型 | 打包展示输入号码牌 | 设置打包场景下是否展示输入取餐实体号码牌页面 |
| 503 | eMenu | 服务设置 | 收据 | 送餐到桌餐牌号获取方式 | 设置送餐到桌餐牌号获取方式是输入实体号码牌还是使用虚拟的订单号 |
| 581 | eMenu | 全局设置 | 下单设置 | 菜单送厨方式 | 设置菜单下单后自动送厨还是需要服务员在POS上手动操作送厨 |
| 502 | eMenu | 服务设置 | 收据 | 可自动送厨的订单支付状态 | 设置订单在何种支付状态下自动送厨 |

### 4.13 食客登记与会员（`foh-guest-registration`）

**归类理由**：点单前会员身份、短信验证码，及手机号/姓名登记与隐私条款默认勾选。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 623 | eMenu | 全局设置 | 用户设置 | 点单前会员身份策略 | 各产线（Kiosk/eMenu/SDI/Online Order）点单前会员身份：可选登录、强制登录/注册，或不展示登录页；每产线三选一；合并原 seq ... |
| 622 | eMenu | 全局设置 | 用户设置 | 登录/注册需要短信验证码 | 默认开启（无开关）：在选定产线（Kiosk/eMenu/CDS/SDI/Online Order）的会员登录/注册页要求短信验证码（须开通会员且该产线 6... |
| 504 | eMenu | 服务设置 | 用户信息 | 展示输入手机号页面 | 食客端（Kiosk/eMenu/扫码等）是否展示输入手机号页面；合并原 seq 30（终端/食客端）为 SSOT。 |
| 505 | eMenu | 服务设置 | 用户信息 | 手机号必填 | 勾选产线在输入手机号页面要求手机号必填（取餐联络等；须与 seq 504 展示页同轨）。 |
| 506 | eMenu | 服务设置 | 用户信息 | 输入姓名 | 勾选产线展示输入食客姓名页面。 |
| 507 | eMenu | 服务设置 | 用户信息 | 姓名必填 | 勾选产线在输入姓名页面要求姓名必填（叫号等）；须与「展示输入姓名页面」配套。 |
| 510 | eMenu | 服务设置 | 用户信息 | 默认选中隐私条款 | 勾选产线在输入手机号页面的隐私条款默认勾选（食客仍可取消）。 |

### 4.14 点单前限制与授权（`foh-guest-pre-order`）

**归类理由**：限制提前开单、下单前服务员授权、切换品类授权。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 620 | eMenu | 全局设置 | 用户设置 | 限制食客提前开单 | 开启后在所选产线（eMenu、SDI）限制食客直接开单（需服务员授权并绑定小费）。 |
| 626 | eMenu | 全局设置 | 用户设置 | 下单前,需要服务员授权 | 开启后在所选产线（eMenu、SDI）食客下单前需要服务员授权。 |
| 627 | eMenu | 全局设置 | 用户设置 | 下单前允许食客切换品类 | 开启后在所选产线（eMenu、SDI）允许食客下单前切换品类菜单；关闭时需服务员授权。 |

### 4.15 食客端语言（`foh-guest-facing-locale`）

**归类理由**：食客端（eMenu/Kiosk/客显）可选语言列表与默认语言（652/653）。


**组内展示顺序**：652 可选语言 → 653 默认语言。

**SSOT**：652/653；不含 467/513/514（见 §3.14）。**109** 为店员端，归系统设置。

**652 选择语言**：多选（英语、中文简体、中文繁体、法语、日语、俄语、西班牙语、越南语、泰语、韩语；默认勾选英语+中文简体）。**653 默认语言**：单选，选项随 652 已选语言联动（原型 localStorage）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 652 | eMenu/Kiosk/客显 | 全局设置 | 多语言 | 选择语言 | 设置终端支持的语言列表 |
| 653 | eMenu/Kiosk/客显 | 全局设置 | 多语言 | 默认语言 | 设置终端的默认语言 |

### 4.16 点餐首页与入口（`foh-guest-menu-home`）

**归类理由**：菜单组、版式框架（eMenu Pro）、品类/分类浏览结构、纯展示、开始按钮与品牌露出。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 599 | eMenu | 全局设置 | 展示设置 | 选择您想展示的菜单组 | 按产线（Kiosk、eMenu、SDI、Online Order）配置要展示的菜单组；点击选择菜单组，以对话框勾选；未选择菜单组的产线不生效。 |
| 604 | eMenu | 全局设置 | 展示设置 | eMenu Pro模式 | 设置所有设备/桌子的菜单页面展示形式（普通/高级设计稿） |
| 601 | eMenu | 全局设置 | 展示设置 | 品类模式 | 开启后，在选定产线（eMenu、SDI）使用品类先分类再选菜的点单形式（非普通扁平菜单）。 |
| 602 | eMenu | 全局设置 | 展示设置 | 菜单分类模式 | 开启后，在选定产线（eMenu、SDI）按菜单分类层级先浏览再选菜（非普通扁平菜单）。 |
| 600 | eMenu | 全局设置 | 展示设置 | 纯展示模式 | 勾选产线后，菜单为纯展示（不可加购）。 |
| 611 | eMenu | 全局设置 | 展示设置 | 展示开始按钮 | 勾选产线后，在首页展示「开始点单」入口。 |
| 532 | eMenu | 服务设置 | 展示设置 | 展示MenuSifu品牌LOGO | LOGO 图片素材请在素材中心维护；本项仅控制是否在选定产线首页展示 MenuSifu 品牌标识。 |

### 4.17 菜单与购物车展示（`foh-guest-menu-body`）

**归类理由**：菜单页视觉（类名/图片/详情/字号）、积分露出、菜单树导航/瀑布流，及购物车展示。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 516 | eMenu | 服务设置 | 菜单 | 显示组名称 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI、Kiosk、Online Order）的组-类-菜树形菜单中展示组名称。 |
| 518 | eMenu | 服务设置 | 菜单 | 默认展开第一组 | 开启后，在选定产线（eMenu、SDI、Kiosk、Online Order）的组-类-菜树形菜单中默认展开第一组下的类。 |
| 606 | eMenu | 全局设置 | 展示设置 | 展示菜单类名称 | 开启后，在选定产线（eMenu、SDI、Kiosk、Online Order）的菜单树形结构中展示类名称。 |
| 517 | eMenu | 服务设置 | 菜单 | 菜单展示位置 | 各产线（eMenu、SDI、Kiosk、Online Order）可独立启用，并选择顶部展示或侧边展示。 |
| 520 | eMenu | 服务设置 | 菜单 | 套餐展示导航栏 | 开启后，在 Kiosk 套餐页面展示步骤分类导航栏。 |
| 608 | eMenu | 全局设置 | 展示设置 | 展示菜详情 | 开启后，按产线配置菜详情展示；点击选择商品，以组/类/菜结构勾选。 |
| 515 | eMenu | 服务设置 | 菜单 | 展示菜单序号 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI、Kiosk、Online Order）的菜单上展示菜品序号 ID。 |
| 528 | eMenu | 服务设置 | 菜单 | 菜价为0展示价格 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI、Kiosk、Online Order）对价格为 0 的菜品展示价格标签。 |
| 618 | eMenu | 全局设置 | 展示设置 | 菜品售罄自动隐藏 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI、Kiosk、Online Order）自动隐藏已售罄菜品。 |
| 616 | eMenu | 全局设置 | 展示设置 | 展示菜单送厨状态 | 开启后，在选定产线（eMenu、SDI）购物车中展示菜单送厨状态。 |
| 524 | eMenu | 服务设置 | 菜单 | 瀑布流模式 | 开启后，在选定产线（Kiosk、eMenu、SDI、Online Order）菜单以瀑布流连续滚动方式浏览。 |
| 607 | eMenu | 全局设置 | 展示设置 | 菜单图片展示模式 | 按产线独立启用菜单图片展示模式（默认/大图模式/小图模式）及例外菜品。 |
| 519 | eMenu | 服务设置 | 菜单 | 菜单图片裁切显示 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI、Kiosk、Online Order）将菜单图片填充菜单卡片（裁切）展示。 |
| 645 | eMenu | 全局设置 | 菜单样式 | 菜品名称字体大小 | 为各产线分别设置菜品名称字号（8–72px，默认 16）；取消勾选表示该产线不启用此设置。 |
| 509 | eMenu | 服务设置 | 用户信息 | 展示账户积分 | 开启后，在选定产线（eMenu、SDI、Kiosk、Online Order、CDS）展示会员账户积分余额。 |
| 525 | eMenu | 服务设置 | 菜单 | 菜单页面展示积分菜 | 勾选产线在菜单页展示可积分兑换的商品。 |
| 526 | eMenu | 服务设置 | 菜单 | 菜单页面积分菜展示位置 | 各产线（eMenu、SDI、Kiosk、Online Order）可独立启用，并选择顶部展示或底部展示。 |
| 617 | eMenu | 全局设置 | 展示设置 | 购物车展示订单价格 | 开启后，在选定产线（eMenu、SDI、Kiosk、Online Order）购物车中展示订单价格。 |

### 4.18 火锅点餐（`foh-guest-hotpot`）

**归类理由**：锅底必选/下单方式/加收与下单后仍展示；无火锅业态可跳过。


**组内展示顺序**：572 锅底必选 → 574 下单方式 → 573 下单后仍展示 → 575 过半加收。

**572–575、573** 右侧**开关** + 产线多选（原型 localStorage）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 572 | eMenu | 全局设置 | 下单设置 | 火锅锅底必选 | 开启后在所选产线（eMenu、SDI）下单时火锅锅底为必选项；关闭则锅底可选。 |
| 574 | eMenu | 全局设置 | 下单设置 | 火锅锅底下单方式 | 开启后按产线（eMenu、SDI）独立配置火锅锅底下单方式（二选一）：锅底先下单，或锅底加入购物车后与其他菜品统一下单。 |
| 573 | eMenu | 全局设置 | 下单设置 | 火锅锅底下单后仍展示锅底 | 开启后，在选定产线（eMenu、SDI）锅底下单后菜单中仍展示火锅锅底品类。 |
| 575 | eMenu | 全局设置 | 下单设置 | 同一锅型,相同锅底超过一半默认加收 | 开启后可添加多条加收规则：列表展示摘要，添加/编辑弹框配置额外加收金额，并按产线（eMenu、SDI）以组/类/菜结构选择适用商品（对齐展示菜详情）。 |

### 4.19 计时与自助餐规则（`foh-guest-duration-scenarios`）

**归类理由**：按时计价场景、自助餐品类先下单与用餐时长展示/提示/禁下单；无 KTV/自助餐/限时用餐可跳过。


**组内展示顺序**：443 按时计价 → 571 品类先下单 → 674 用餐时长限制 → 577 展示用餐时长 → 578 倒计时 → 579 剩余提示 → 580 提示后禁下单。

**674** 无总开关，按 POS、POS GO、PayPad、eMenu、SDI 分别启用并设置 1–1440 分钟；**577–580** 保留总开关并扩展为相同五产线，未启用限制的产线自动失效但保留选择。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 443 | 前厅 | 按时计价 | 按时计价 | 按照时长收费 | 开启后多选适用场景（KTV、VIP包间）；勾选后对应场景按时长收费。 |
| 571 | eMenu | 全局设置 | 下单设置 | 品类先下单 | 开启后在所选产线（eMenu、SDI）品类/自助餐模式下，选择套餐后默认先下单再选套餐内商品。 |
| 674 | 前厅 | 虚拟设置 | 下单设置 | 用餐时长限制 | 按 POS、POS GO、PayPad、eMenu、SDI 分别启用并设置用餐时长限制（1–1440 分钟）；关联的展示、倒计时、提示和禁下单功能仅在已启... |
| 577 | eMenu | 全局设置 | 下单设置 | 展示用餐时长 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI）订单下单后展示用餐时长；对应产线须先启用用餐时长限制。 |
| 578 | eMenu | 全局设置 | 下单设置 | 用餐时长倒计时展示 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI）将用餐时长以倒计时方式展示；对应产线须先启用用餐时长限制。 |
| 579 | eMenu | 全局设置 | 下单设置 | 用餐剩余时长提示 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI）于剩余用餐时长达到阈值时弹出提示；对应产线须先启用用餐时长限制。 |
| 580 | eMenu | 全局设置 | 下单设置 | 用餐剩余时长提示后不允许下单 | 开启后，在选定产线（POS、POS GO、PayPad、eMenu、SDI）于用餐剩余时长提示后禁止食客自助下单；对应产线须先启用用餐时长限制。 |

### 4.20 桌边呼叫（`foh-tableside-service`）

**归类理由**：桌边呼叫服务员及服务类型。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 641 | eMenu | 全局设置 | 消息通知 | 未开单可呼叫服务员 | 开启后，在选定产线（eMenu、SDI）未开单（无有效订单）时仍允许发起桌边服务请求。 |
| 640 | eMenu | 全局设置 | 消息通知 | 呼叫服务员时间间隔 | 按产线（eMenu、SDI）启用并配置同一桌/会话重复发起呼叫的最小间隔（秒）；0 表示启用但不限制间隔。 |
| 333 | 前厅 | 系统功能 | 消息中心 | 用餐者请求服务的类型 | 按产线（eMenu、SDI）多选客人可发起的桌边服务请求类型（呼叫服务员/结账/加水等），支持弹窗新增与删除自定义类型；原 seq 629、630–636... |

### 4.21 点单备注（`foh-guest-order-notes`）

**归类理由**：订单、商品与套餐子项备注开关。


**SSOT 备注**：521 订单、522 商品、523 套餐子项。

**521–523** 右侧**开关**（保留功能场景描述与原存储）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 521 | eMenu | 服务设置 | 菜单 | 订单备注 | 开启后在所选产线允许食客填写订单级特殊备注。适用产线：Kiosk、eMenu、SDI、Online Order。 |
| 522 | eMenu | 服务设置 | 菜单 | 产品备注 | 开启后，按产线配置支持食客特殊备注的商品；点击选择商品，以组/类/菜结构勾选。 |
| 523 | eMenu | 服务设置 | 菜单 | 套餐子项备注 | 设置套餐内的子商品是否支持食客进行特殊备注 |

### 4.22 等待时长计算与展示（`foh-wait-time-display`）

**归类理由**：Kiosk / Online Order 点单页预计等待时长展示、区间规则与样式；主开关 + 产线多选。


**535/536** 主开关 + 子项（分钟/杯数）；**537** 多选（排队数量/等待时间）；**538** 字体大小（默认/倍数）；**539/540** 字体背景色/颜色（默认/自定义色块）。

| seq | 终端 | 原导航 | 功能模块 | 功能设置 | 功能场景描述（摘要） |
|-----|------|--------|----------|----------|----------------------|
| 673 | 前厅 | 虚拟设置 | 等待时长 | 预计等待时长计算设置 | 全店统一的排队杯数→预计等待时长换算规则；各产线展示开关、区间与样式均依据本设置计算。 |
| 535 | eMenu | 服务设置 | 等待时长 | 展示当前订单预计等待时长 | 按产线（Kiosk）启用并配置功能时间：自动关闭提示与菜单弹框阈值（分钟）；时长依据「预计等待时长计算设置」。 |
| 536 | eMenu | 服务设置 | 等待时长 | 预计等待时长区间设置 | 按产线（Kiosk）启用并配置区间功能时间：杯数/等待时长阈值与区间起止偏移（分钟）；时长依据「预计等待时长计算设置」。 |
| 537 | eMenu | 服务设置 | 等待时长 | 等待时长展示类型 | 按产线（Kiosk）配置展示类型：排队数量与/或等待时间；时长依据「预计等待时长计算设置」。 |
| 538 | eMenu | 服务设置 | 等待时长 | 字体大小 | 按产线（Kiosk）配置字体大小：系统默认或倍数。 |
| 539 | eMenu | 服务设置 | 等待时长 | 字体背景色 | 按产线（Kiosk）配置字体背景色：系统默认或自定义色。 |
| 540 | eMenu | 服务设置 | 等待时长 | 字体颜色 | 按产线（Kiosk）配置字体颜色：系统默认或自定义色。 |

---

## 5. 本次分组合并对照（25 个规范组 + 孤立折扣 → 22 组）

| 新 groupTitle | 吸收的旧分组示例 |
|---------------|------------------|
| POS 菜单与界面 | 菜单查找与时段、菜单区界面布局 |
| 点单页工具栏 | 原工具栏配置、自定义分割线名称 |
| 找单与结账入口 | POS 找单列表、POS 结账入口 |
| 订单类型、取餐与送厨 | 订单类型与取餐、食客端送厨 |
| 桌边呼叫 | 原桌边服务中的呼叫与服务类型 |
| 点单备注 | 原桌边服务中的订单/商品/套餐子项备注 |
| 消息类型提醒 | POS 通知总控、订单消息提醒、点单超时提醒 |

---

## 6. 落地步骤

1. 按本文 **第 4 节** 更新 `docs/项目文档/配置归类-分组映射.csv` 中对应 `seq` 的 `groupTitle`、`groupKey`
2. 执行：`node scripts/apply-foh-settings-mapping.mjs`（可选，与本文映射一致）
3. 执行：`cd admin-web && npm run build:settings-catalog`
4. 执行：`apply-order-settings-mapping.mjs`、`apply-product-settings-mapping.mjs`、`apply-payment-settings-mapping.mjs`
5. 在前厅 / 订单 / 商品 / 支付中心 → 设置 页验证二级导航

### 6.1 映射表（可直接写入 CSV）

```csv
seq,groupTitle,groupKey
75,登录与终端主界面,foh-pos-shell
107,选桌与开台流程,foh-table-start-flow
110,消息类型提醒,foh-pos-order-alerts
111,选桌与开台流程,foh-table-start-flow
113,送厨规则与权限,foh-kitchen-send-timing
114,送厨规则与权限,foh-kitchen-send-timing
118,POS 菜单与界面,foh-pos-menu-scope
120,送厨规则与权限,foh-kitchen-send-timing
121,点单内容与客户信息,foh-pos-order-cart
122,点单内容与客户信息,foh-pos-order-cart
123,送厨规则与权限,foh-kitchen-send-timing
125,送厨规则与权限,foh-kitchen-send-timing
132,点单内容与客户信息,foh-pos-order-cart
133,点单内容与客户信息,foh-pos-order-cart
135,点单内容与客户信息,foh-pos-order-cart
137,点单内容与客户信息,foh-pos-order-cart
138,套餐与自定义点单,foh-pos-combo-ordering
139,套餐与自定义点单,foh-pos-combo-ordering
141,点单内容与客户信息,foh-pos-order-cart
145,套餐与自定义点单,foh-pos-combo-ordering
148,POS 菜单与界面,foh-pos-menu-scope
151,找单与结账入口,foh-pos-find-order-list
152,找单与结账入口,foh-pos-find-order-list
153,找单与结账入口,foh-pos-find-order-list
165,登录与终端主界面,foh-pos-shell
166,登录与终端主界面,foh-pos-shell
174,POS 菜单与界面,foh-pos-menu-scope
175,登录与终端主界面,foh-pos-shell
178,点单内容与客户信息,foh-pos-order-cart
193,点单页按钮显隐,foh-pos-buttons
194,点单页按钮显隐,foh-pos-buttons
195,点单页按钮显隐,foh-pos-buttons
196,点单页工具栏,foh-pos-order-toolbar
197,点单页按钮显隐,foh-pos-buttons
198,点单页按钮显隐,foh-pos-buttons
199,点单页按钮显隐,foh-pos-buttons
200,点单页按钮显隐,foh-pos-buttons
201,点单页按钮显隐,foh-pos-buttons
202,点单页按钮显隐,foh-pos-buttons
203,点单页按钮显隐,foh-pos-buttons
204,点单页按钮显隐,foh-pos-buttons
205,点单页按钮显隐,foh-pos-buttons
206,点单页按钮显隐,foh-pos-buttons
207,点单页按钮显隐,foh-pos-buttons
208,点单页按钮显隐,foh-pos-buttons
209,点单页按钮显隐,foh-pos-buttons
210,点单页按钮显隐,foh-pos-buttons
211,点单页按钮显隐,foh-pos-buttons
212,点单页按钮显隐,foh-pos-buttons
213,点单页按钮显隐,foh-pos-buttons
214,点单页按钮显隐,foh-pos-buttons
215,点单页按钮显隐,foh-pos-buttons
216,POS 菜单与界面,foh-pos-menu-scope
217,POS 菜单与界面,foh-pos-menu-scope
218,POS 菜单与界面,foh-pos-menu-scope
219,POS 菜单与界面,foh-pos-menu-scope
220,POS 菜单与界面,foh-pos-menu-scope
221,找单与结账入口,foh-pos-find-order-list
222,点单内容与客户信息,foh-pos-order-cart
223,点单内容与客户信息,foh-pos-order-cart
248,找单与结账入口,foh-pos-find-order-list
251,找单与结账入口,foh-pos-find-order-list
331,消息类型提醒,foh-pos-order-alerts
332,消息类型提醒,foh-pos-order-alerts
333,桌边呼叫,foh-tableside-service
345,送厨规则与权限,foh-kitchen-send-timing
346,登录与终端主界面,foh-pos-shell
347,清桌与换服务员（企台）,foh-table-clear-ops
348,POS 菜单与界面,foh-pos-menu-scope
349,点单内容与客户信息,foh-pos-order-cart
350,POS 菜单与界面,foh-pos-menu-scope
351,清桌与换服务员（企台）,foh-table-clear-ops
443,计时与自助餐规则,foh-guest-duration-scenarios
483,点单页工具栏,foh-pos-order-toolbar
484,点单页工具栏,foh-pos-order-toolbar
485,点单页工具栏,foh-pos-order-toolbar
486,点单页工具栏,foh-pos-order-toolbar
487,订单类型、取餐与送厨,foh-guest-order-type
488,订单类型、取餐与送厨,foh-guest-order-type
489,订单类型、取餐与送厨,foh-guest-order-type
490,订单类型、取餐与送厨,foh-guest-order-type
491,订单类型、取餐与送厨,foh-guest-order-type
502,订单类型、取餐与送厨,foh-guest-order-type
503,订单类型、取餐与送厨,foh-guest-order-type
504,食客登记与会员,foh-guest-registration
505,食客登记与会员,foh-guest-registration
506,食客登记与会员,foh-guest-registration
507,食客登记与会员,foh-guest-registration
509,菜单与购物车展示,foh-guest-menu-body
510,食客登记与会员,foh-guest-registration
515,菜单与购物车展示,foh-guest-menu-body
516,菜单与购物车展示,foh-guest-menu-body
517,菜单与购物车展示,foh-guest-menu-body
518,菜单与购物车展示,foh-guest-menu-body
519,菜单与购物车展示,foh-guest-menu-body
520,菜单与购物车展示,foh-guest-menu-body
521,点单备注,foh-guest-order-notes
522,点单备注,foh-guest-order-notes
523,点单备注,foh-guest-order-notes
524,菜单与购物车展示,foh-guest-menu-body
525,菜单与购物车展示,foh-guest-menu-body
526,菜单与购物车展示,foh-guest-menu-body
528,菜单与购物车展示,foh-guest-menu-body
532,点餐首页与入口,foh-guest-menu-home
534,清桌与换服务员（企台）,foh-table-clear-ops
535,等待时长计算与展示,foh-wait-time-display
536,等待时长计算与展示,foh-wait-time-display
537,等待时长计算与展示,foh-wait-time-display
538,等待时长计算与展示,foh-wait-time-display
539,等待时长计算与展示,foh-wait-time-display
540,等待时长计算与展示,foh-wait-time-display
571,计时与自助餐规则,foh-guest-duration-scenarios
572,火锅点餐,foh-guest-hotpot
573,火锅点餐,foh-guest-hotpot
574,火锅点餐,foh-guest-hotpot
575,火锅点餐,foh-guest-hotpot
577,计时与自助餐规则,foh-guest-duration-scenarios
578,计时与自助餐规则,foh-guest-duration-scenarios
579,计时与自助餐规则,foh-guest-duration-scenarios
580,计时与自助餐规则,foh-guest-duration-scenarios
581,订单类型、取餐与送厨,foh-guest-order-type
592,选桌与开台流程,foh-table-start-flow
599,点餐首页与入口,foh-guest-menu-home
600,点餐首页与入口,foh-guest-menu-home
601,点餐首页与入口,foh-guest-menu-home
602,点餐首页与入口,foh-guest-menu-home
604,点餐首页与入口,foh-guest-menu-home
606,菜单与购物车展示,foh-guest-menu-body
607,菜单与购物车展示,foh-guest-menu-body
608,菜单与购物车展示,foh-guest-menu-body
611,点餐首页与入口,foh-guest-menu-home
616,菜单与购物车展示,foh-guest-menu-body
617,菜单与购物车展示,foh-guest-menu-body
618,菜单与购物车展示,foh-guest-menu-body
619,选桌与开台流程,foh-table-start-flow
620,点单前限制与授权,foh-guest-pre-order
621,选桌与开台流程,foh-table-start-flow
622,食客登记与会员,foh-guest-registration
623,食客登记与会员,foh-guest-registration
625,选桌与开台流程,foh-table-start-flow
626,点单前限制与授权,foh-guest-pre-order
627,点单前限制与授权,foh-guest-pre-order
637,消息类型提醒,foh-pos-order-alerts
638,消息类型提醒,foh-pos-order-alerts
639,消息类型提醒,foh-pos-order-alerts
640,桌边呼叫,foh-tableside-service
641,桌边呼叫,foh-tableside-service
642,清桌与换服务员（企台）,foh-table-clear-ops
643,选桌与开台流程,foh-table-start-flow
645,菜单与购物车展示,foh-guest-menu-body
652,食客端语言,foh-guest-facing-locale
653,食客端语言,foh-guest-facing-locale
673,等待时长计算与展示,foh-wait-time-display
674,计时与自助餐规则,foh-guest-duration-scenarios
```

---

## 7. 边界说明

- **送厨时机、支付方式、税率** 等以订单/支付/商品中心为主入口；前厅保留界面与交互项。
- **客显屏**（`cds`）：461/462/466；与素材中心、guest-menu-global 分工见 §3.15。
- **C 端界面语言**（652/653）在 `guest-facing-locale`；**小费页、小票**（463、465）在支付中心。
- **菜单文案多语言**（456）在商品中心；**店员默认语言**（109）在系统设置。
- 当前复杂版本前厅 catalog 共 154 项，均已列入上文；seq 164、176、177 已退役并排除。
- **107/533** 选桌、**108/619** 人数页已合并至前厅；**126** 默认单型留订单中心；**593** 归商品管理。
- **v1.25 自系统设置迁入**：**165** → `pos-shell-landing`（POS 登录默认主界面；门店 × 产线兜底；UI：`module-settings-default-main-screen-ui.ts`）。
- **v1.24 自会员中心迁入**：**622** → `guest-order-rules`（紧挨 **623**；开通会员后各产线登录/注册是否须短信验证码；UI：`module-settings-member-sms-verification-ui.ts`）。
- **v1.23 自会员中心迁入**：**505/507/510** → `guest-order-rules`（与 **504/506** 输入页配套：必填与隐私默认；取餐联络，非会员专属；UI：`module-settings-member-registration-fields-ui.ts`）。
- **v1.22 自会员中心迁入**：**623** → `guest-order-rules`（点单前登录门禁，紧挨 504/506；UI：`module-settings-member-login-policy-ui.ts`）。
- **v1.21 自会员中心迁入**：**509** → `guest-menu-global`；**525/526** → `guest-menu-structure`；**527** → `guest-order-rules`（UI：`module-settings-member-points-rewards-ui.ts`）。
- **v1.20 自会员中心迁入**：**10** → `cds`；**222/223** → `pos-order-cart`；**504/506** → `guest-order-rules`（504 合并原 **30**）。

